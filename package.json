{
  "name": "one-nation-one-athlete",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "setup": "npm install --prefix frontend && npm install --prefix backend",
    "dev": "concurrently -k -n API,WEB \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\"",
    "build": "npm run build --prefix frontend",
    "test": "npm test --prefix backend",
    "test:e2e": "npm run test:e2e --prefix frontend",
    "format": "prettier --write frontend/src backend/src backend/tests docs README.md"
  },
  "devDependencies": {
    "concurrently": "^10.0.5",
    "prettier": "^3.8.1",
    "sharp": "^0.35.4"
  }
}
