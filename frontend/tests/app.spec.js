import {test,expect} from '@playwright/test';
test('landing, routed workspace, CRUD, offline sync, and mobile layouts',async({page,context})=>{const errors=[];page.on('pageerror',e=>errors.push(e.message));await page.goto('/');await expect(page.getByRole('heading',{name:/A BILLION/})).toBeVisible();await page.screenshot({path:'artifacts/landing-desktop.png'});await page.getByRole('button',{name:'Explore demo',exact:true}).click();await expect(page.getByRole('heading',{name:/Let.s go, Aarav/})).toBeVisible();await page.screenshot({path:'artifacts/dashboard-desktop.png'});await page.getByRole('button',{name:'Log a session'}).click();await page.getByLabel('Session title').fill('Browser sprint');await page.getByLabel('Measured result').fill('11.9');await page.getByRole('button',{name:'Save record'}).click();await expect(page.getByRole('dialog')).toHaveCount(0);await page.getByRole('link',{name:'Performance',exact:true}).click();const row=page.getByRole('row').filter({hasText:'Browser sprint'});await expect(row).toBeVisible();await row.getByRole('button',{name:'Edit record'}).click();await page.getByLabel('Measured result').fill('11.8');await page.getByRole('button',{name:'Save record'}).click();await expect(row).toContainText('11.8');await row.getByRole('button',{name:'Delete record'}).click();await page.getByRole('button',{name:'Delete permanently',exact:true}).click();await expect(row).toHaveCount(0);
 for(const name of ['Athlete passport','Video lab','Training plan','Recovery','Opportunities','Financial hub','Career pathways','Transparency','Profile & privacy']){await page.getByRole('link',{name,exact:true}).click();await expect(page.locator('main h1')).toBeVisible();}
 await page.getByRole('link',{name:'Training plan',exact:true}).click();await page.getByRole('button',{name:'Save this weekly plan'}).click();await expect(page.getByText('0 / 7 days complete')).toBeVisible();await page.getByRole('checkbox').first().check();await expect(page.getByText('1 / 7 days complete')).toBeVisible();await page.getByRole('link',{name:'Opportunities',exact:true}).click();await page.getByRole('button',{name:'Try demo application'}).first().click();await expect(page.getByRole('button',{name:'Withdraw demo application'})).toBeVisible();await page.getByRole('button',{name:'Withdraw demo application'}).click();
 await page.getByRole('link',{name:'Performance',exact:true}).click();await context.setOffline(true);await page.getByRole('button',{name:'Log session',exact:true}).click();await page.getByLabel('Session title').fill('Offline sprint');await page.getByRole('button',{name:'Save record'}).click();await expect(page.getByText('1 offline session(s) waiting to sync.')).toBeVisible();await context.setOffline(false);await expect(page.getByRole('row').filter({hasText:'Offline sprint'})).toBeVisible({timeout:15000});await page.reload();await expect(page.getByRole('heading',{name:'Progress over promises.'})).toBeVisible();
 await page.setViewportSize({width:390,height:844});await page.getByRole('button',{name:'Open navigation'}).click();await page.getByRole('link',{name:'Overview',exact:true}).click();await expect(page.locator('.sidebar')).not.toHaveClass(/visible/);if(await page.getByRole('button',{name:'Dismiss notification'}).isVisible())await page.getByRole('button',{name:'Dismiss notification'}).click();await page.screenshot({path:'artifacts/dashboard-mobile.png'});expect(await page.evaluate(()=>document.documentElement.scrollWidth<=innerWidth)).toBe(true);await page.getByRole('button',{name:'Open navigation'}).click();await page.getByRole('button',{name:'Log out'}).click();await expect(page.getByRole('heading',{name:/A BILLION/})).toBeVisible();await page.screenshot({path:'artifacts/landing-mobile.png'});expect(await page.evaluate(()=>document.documentElement.scrollWidth<=innerWidth)).toBe(true);expect(errors).toEqual([]);
});
